# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Supabase Configurations (Client-side variables start with VITE_)
# Add these in AI Studio Settings -> Secrets to connect your live database!
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase PostgreSQL Connection (For PHP / Backend PDO database connection)
DB_HOST=
DB_PORT=5432
DB_DATABASE=postgres
DB_USERNAME=postgres
DB_PASSWORD=

